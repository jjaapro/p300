numpy>=1.24
# Windows: Python's zoneinfo module needs an IANA tzdata source. Linux/Mac
# read /usr/share/zoneinfo; Windows has no equivalent, so we depend on the
# tzdata PyPI package. Required by services/fomc_service.py for DST-aware
# FOMC announcement timing (14:00 ET -> UTC).
tzdata; sys_platform == "win32"

# AI_QUANT sleeve: matplotlib renders the per-decision chart that the
# multimodal LLM looks at; mplfinance draws the OHLC candles. anthropic is
# the SDK used by strategies.sleeves.ai_quant.decision to call Claude. feedparser
# is the RSS/Atom parser used by data.sources.news to ingest
# headlines from a hand-picked list of reputable outlets directly,
# instead of going through a third-party aggregator.
matplotlib>=3.8
mplfinance>=0.12.10b0
anthropic>=0.40
feedparser>=6.0

# dashboard/: psutil is the ground-truth process scan behind the fleet
# panel's duplicate-instance detection (dashboard/procscan.py). The scan
# collapses the Windows venv-shim parent/child pair — see that module's
# docstring before touching anything process-count related.
psutil>=7.0
