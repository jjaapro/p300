# Copy to .env and fill in. .env is gitignored.

# Coinalyze API key (free tier — register at https://coinalyze.net/).
# Used by fetch_coinalyze.py for the one-shot LSR history fetch during
# bootstrap or to fill historical gaps that Binance won't serve (>30d).
# Not needed for ongoing live operation.
COINALYZE_API_KEY=

# MEXC credentials — NOT USED in paper mode. The bot writes phantom
# trades to data/dashboard.db only. Keys are included here so a future
# primary-promotion path has a place to plug in without schema churn.
MEXC_API_KEY=
MEXC_SECRET=

# services/news_fetcher.py pulls headlines from a hand-picked list of
# reputable RSS feeds (CoinDesk, The Block, Decrypt, Bitcoin Magazine,
# BBC Business, CNBC). RSS is static XML — no auth tokens, no rate
# limits, no third-party aggregator. The default source list lives in
# services/news_fetcher.py:SOURCES; edit it there to add or drop a feed.
# (No env var needed for news ingest.)

# Anthropic API key for the AI_QUANT sleeve's daily decision call.
# claude-opus-4-7 (default) at ~$0.50–$2 per decision with prompt caching.
# Without this, the sleeve refuses to fire. Cap daily spend with
# AI_QUANT_DAILY_COST_CAP_USD (default 5.0).
ANTHROPIC_API_KEY=
AI_QUANT_MODEL=claude-opus-4-7
AI_QUANT_DAILY_COST_CAP_USD=5.0
AI_QUANT_ENABLED=false

# Reasoning-effort level for the AI_QUANT decision call. Opus 4.7 uses
# adaptive thinking — without an explicit hint, the model often skips
# deliberation on routine prompts. For a daily money-on-the-line
# decision we want step-by-step verification, so this is set to "high".
# The model still adaptively decides how much of the effort budget to
# use; this is a hint, not a floor.
#
# low	Efficient, but best for short, scoped tasks. Pair low with explicit checklists if your task has multiple sections.
# medium	The drop-in for the average workflow where you want good results while reducing costs.
# high	Advanced use cases that still need a balance of intelligence and token consumption. This is often the sweet spot balancing quality and token efficiency.
# xhigh	The recommended starting point for coding and agentic work, and for exploratory tasks such as repeated tool calling, detailed web search, and knowledge-base search. Expect meaningfully higher token usage than high.
# max	Reserve for genuinely frontier problems. On most workloads max adds significant cost for relatively small quality gains, and on some structured-output or less intelligence-sensitive tasks it can lead to overthinking.

AI_QUANT_EFFORT=xhigh
